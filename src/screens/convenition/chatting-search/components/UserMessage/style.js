import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {},
  message: {
    color: '#000',
    fontSize: 18,
    fontWeight: '400',
    backgroundColor: '#ccc',
    paddingHorizontal: 16,
    paddingVertical:4,
    borderRadius: 15
  }
})
export { styles }
