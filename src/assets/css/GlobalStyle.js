import { StyleSheet } from 'react-native'

const GlobalStyle = StyleSheet.create({
  container: {
    flex: 1
  },

  backgroundTesting: {
    backgroundColor: '#d11'
  }
})

export default GlobalStyle
